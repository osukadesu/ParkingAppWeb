import { FiltroEstacionamientoPipe } from './filtro-estacionamiento.pipe';

describe('FiltroEstacionamientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstacionamientoPipe();
    expect(pipe).toBeTruthy();
  });
});
