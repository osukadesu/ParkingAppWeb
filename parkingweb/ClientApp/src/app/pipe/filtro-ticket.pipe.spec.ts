import { FiltroTicketPipe } from './filtro-ticket.pipe';

describe('FiltroTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
